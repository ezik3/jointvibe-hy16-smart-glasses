# Xcode user-specific state (never useful to share/version)
xcuserdata/
*.xcuserstate

# Build products / derived data
DerivedData/
build/

# macOS Finder metadata
.DS_Store

# Local secrets (OpenRouter API key, model config) - NEVER commit.
# See HY16Probe/Secrets.plist.example for the tracked template.
HY16Probe/Secrets.plist
